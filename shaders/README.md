# Reference WGSL

Primary shader sources are embedded in the Rust renderer (`engine/renderer`).

| File | Role |
| --- | --- |
| `rect.wgsl` | Reference copy of the instanced rectangle + outline shaders |

Built-in procedural shaders live in `engine/renderer/src/shader_lib.rs`.
